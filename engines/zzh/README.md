# ZZH 权重

将训练得到的 `<mode>.pt` 放在本目录（`jieqi`、`xiangqi`、`anqi`、`zhencha`、`manchu`、`bawang`、`wuhu`）。

大厅难度选 **ZZH** 时按玩法加载。单文件约 180MB，已加入 `.gitignore`，请勿提交到 Git。

详见 [`../../train/README.md`](../../train/README.md)。
